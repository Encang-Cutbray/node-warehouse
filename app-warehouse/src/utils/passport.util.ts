import LocalStrategy from 'passport-local'
import bcrypt from 'bcrypt'
import model from '../models/index'

export const initialize = (passport: any) => {
	const cb = async (email: string, passport: string, done: Function) => { }
}
