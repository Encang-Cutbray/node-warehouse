<header>
	<nav>
		<div class="nav-wrapper">
			<div class="row">
				<div class="col s12">
					<a href="#" data-target="mobile-sidenav" class="left sidenav-trigger hide-on-medium-and-up"><i
							class="material-icons">menu</i></a>
					<a href="/" class="brand-logo">
						<%= APP_NAME %>
					</a>
					<ul id="nav-mobile" class="right">
						<% if (!isAuth) { %>
							<li class="<%= urlActive === '/login' ? 'active' : null %>"><a href="/login">Login</a></li>
							<li class="<%= urlActive === '/register' ? 'active' : null %>"><a href="/register">Registration</a></li>
						<% } %>
						<% if (isAuth) { %>
							<li class="<%= urlActive === '/logout' ? 'active' : null %>"><a href="/logout">Logout</a></li>
						<% } %>
					</ul>
				</div>
			</div>
		</div>
	</nav>
</header>
